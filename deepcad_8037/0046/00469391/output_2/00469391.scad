
// Define dimensions for the rectangular block
block_length = 80;  // Increased length to elongate the block to match the original model
block_width = 25;   // Reduced width to narrow the block and match the original proportions
block_height = 4;   // Reduced height to make the block thinner and match the original model

// Create the rectangular block
cube([block_length, block_width, block_height]);

