
// Define dimensions for the rectangular block
block_length = 70;  // Increased length to make the block more elongated
block_width = 30;   // Width remains the same
block_height = 5;   // Reduced height to make the block thinner

// Create the rectangular block
cube([block_length, block_width, block_height]);

