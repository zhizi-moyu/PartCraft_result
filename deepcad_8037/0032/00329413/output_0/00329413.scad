module component_name_1() {
    // Dimensions of the rectangular prism
    length = 50;  // Length of the prism
    width = 5;    // Width of the prism
    height = 2;   // Height of the prism

    // Create the rectangular prism
    cube([length, width, height], center = true);
}

// Main function to render the model
component_name_1();
